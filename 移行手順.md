# Character Lens 軽量版の移行手順

## 移行先で行うこと

1. [Python公式サイト](https://www.python.org/downloads/windows/)からPython 3.14.6の64bit版を導入します。Tkinterとpipを含む通常のインストールを使用してください。既存の古いPythonを削除する必要はありません。
2. ZIPをドキュメント等の書き込み可能なフォルダーへ完全に展開します。ZIP内から直接起動しないでください。
3. [Ollama公式サイト](https://ollama.com/download/windows)からWindows版を導入し、起動します。
4. 展開先の **app.py** をPythonで実行します。初回準備画面はPillow等が未導入でも開きます。コマンドを使う場合は `python app.py` です。複数のPythonがある場合は3.14.6を選んでください。
5. 「ライブラリを導入」で記録された版を導入します。このPythonのユーザー領域に導入するため、同じPythonを使う他のアプリにも影響する場合があります。仮想環境やバッチは不要です。
6. 「専用モデルを取得」で約379MBを取得します。「Ollamaモデルを取得」でqwen3-vl:latestを取得します。移行元では約5.7GiBでした。ダウンロードと展開に十分な空き容量・インターネット接続が必要です。
7. 履歴を移したい場合は、**本体アプリを初めて開始する前に**「同梱の履歴を復元」を押します。25枚の画像一覧・26件の解析履歴・設定・確認内容を復元します。
8. 元画像は別途コピーしてください。「移行先の画像フォルダーを指定」で保存先を選びます。サブフォルダーも探します。同名かつ内容のSHA-256が一致する画像だけを結び直し、変更前の履歴をバックアップします。画像自体は変更しません。
9. 必要な項目が揃ったら「アプリを開始」を押します。次回からは通常の画面が開きます。

## 同じ環境を復元するための情報

Python・ライブラリの版はmigration_profile.jsonとrequirements-migration.txtに記録しています。専用モデルは配布元の特定revisionとファイルのSHA-256で検証します。壊れたダウンロードを完成品として使用しません。

Ollamaのlatestタグは将来更新される可能性があります。取得後のモデル識別子を移行元と比較し、違う場合は明示的な確認欄にチェックするまで開始できません。この場合は完全に同じ環境ではなく、解析結果が変わることがあります。既に同名のローカル画像モデルがある場合はそのまま保持します。既存タグを自動更新しません。

Python 3.12以上の64bit版で起動できますが、推奨・検証環境は3.14.6です。他の版ではライブラリが取得できない可能性があります。版違いの確認欄は、欠けているライブラリ・モデルを無視して起動する機能ではありません。

## 途中で止まった場合

- 処理中は画面の記録に進捗を表示します。完了するまで閉じずにお待ちください。取得中のキャンセル機能はありません。
- 失敗した場合はネットワーク・空き容量・Ollamaの起動を確認して再試行します。検証済みの専用モデルは再利用し、未完成の専用モデルは最初から取得し直します。Ollamaは取得済みレイヤーを再利用します。
- プロキシや証明書により取得できない場合は記録にエラーが出ます。証明書検証を無効にしないでください。
- 既存の履歴には上書きしません。本体を開始済みで同梱履歴に戻したい場合は本体を閉じ、現在のdataフォルダーを別名で保管してからapp.pyを再実行してください。現在の履歴との自動統合機能はありません。
- 本体起動中の履歴復元・結び直しは拒否します。本体を閉じてから初回準備画面で操作してください。再表示は本体の「初回準備・移行」、または `python app.py --setup` で行えます。
- 未解決の画像は、ファイル名変更・内容変更・同一ファイルの複数配置がないか確認してください。画像を結び直しても過去のAI結果は書き換えません。

## ZIPに含むもの

コード一式、プロンプト、Schema、名前辞書、固定版の依存情報、専用モデルの検証情報・タグ・ライセンス、解析履歴、会話の要約、説明書、テスト、SHA256SUMS.txt。

元画像、約379MBの専用モデル本体、Ollama本体・モデル、Python本体、認証情報、実行用バッチは含みません。履歴には元パス・解析結果・設定が含まれます。解析時は画像・ファイル名を外部サービスに送信せず、このPCのOllamaを使います。

別の物理PCでの導入は未検証です。このPCの分離した保存先で履歴復元、モデル取得と検証、初回準備画面を確認しています。Ollamaの数GBのモデルをゼロから取得する試験は行っていません。

