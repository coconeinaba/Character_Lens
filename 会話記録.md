# Character Lens 開発・移行についての会話要約

会話で決めた要件と対応を整理したものです。逐語録ではありません。

## 目的と設計

ローカルのOllamaをPythonから使用し、アニメ・漫画・ゲームのキャラクター候補と特徴別の類似度を確認するWindowsアプリを作成する方針となりました。app.pyで起動し、Tkinterのファイル・フォルダー選択を使います。仮想環境やバッチ起動は使用しません。

キャラクター候補は専用のWD ViT Tagger v3で絞り、Ollamaで外見の観察と候補の特徴評価を行います。候補が得られない場合に名前を無理に作らない方針です。自由探索と指定キャラクター評価も用意しました。公式画像を収集・照合する方式ではありません。

髪、顔・目、衣装、固有の目印、配色、体形・種族的特徴の6軸を使います。見えない軸は評価不能とし、Pythonで重み付き集計します。点数や確かさは未校正で、正答確率や実測一致率とは扱いません。実画像で衣装の特徴を誤って評価する例があり、人による確認が必要です。

## 実装と確認

画像追加、フォルダー追加、手動の範囲指定、一括解析、中断・再開、SQLite履歴、過去結果の比較、確認メモ、CSV・HTML・JSON書き出しを実装しました。元画像は変更しません。ローカル画像対応モデルを選び、クラウドモデルを除外します。

実画像を登録して解析し、25枚の一覧と26件の解析履歴を移行用に保存しました。画像表示のちらつきについて、同じ画像の繰り返し読み込みと表示領域のサイズ変化を抑える修正を行いました。

## 別PCへの移行とメール送信

大容量のモデルを含む一式ではメール添付が重くなるため、「軽量な移行用ZIP＋初回準備画面」で進めることと、メールを送ることが明示的に依頼されました。

PythonとOllamaの本体を移行先で導入した後、app.pyの初回準備画面からライブラリ・専用モデル・Ollamaモデルを取得する方式にしました。移行元の版を記録し、専用モデルはSHA-256を検査します。Ollamaモデルの版違いは画面で確認します。既存履歴や既存Ollamaモデルを自動で上書きしません。

履歴復元と画像フォルダーの指定を初回準備画面へ追加しました。画像は別途コピーし、同名かつ同じ内容のファイルだけを結び直します。

指定された送付先に、コード・会話要約・説明書・履歴をまとめた1つの軽量ZIPを添付する方針です。元画像や大容量モデルは添付に含めません。

